version: '3.8'
services:
  ollama:
    image: ghcr.io/ollama/ollama:latest
    ports:
      - "11434:11434"
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - ollama
    environment:
      OLLAMA_HOST: http://ollama:11434
    volumes:
      - .:/workspace
